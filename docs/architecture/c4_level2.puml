@startuml C4_Container_CaMo
' C4-PlantUML Container diagram include (remote)
!includeurl https://raw.githubusercontent.com/plantuml-stdlib/C4-PlantUML/master/C4_Container.puml

' optional: set skinparams oder Theme (siehe Repo für Varianten)

' Titel
LAYOUT_WITH_LEGEND()

' Definiere Actor
Person(person, "Student*in", "studiert an der DHBW Technik und nutzt CaMo für eine effiziente An- und Abfahrt.")

' Definiere System (Boundary) und Container
System_Boundary(camo, "CaMo – Campus Mobility") {

  Container(web, "Web App", "React / PWA", 
    "Stellt die Benutzeroberfläche bereit und kommuniziert über HTTPS mit dem Backend.")

  Container(api, "Backend API", "Java / Spring Boot", 
    "Verarbeitet Suchanfragen, verwaltet Favoriten und sendet E-Mails über das interne E-Mail-System.")

  ContainerDb(db, "Datenbank", "PostgreSQL", 
    "Speichert Nutzerkonten, Favoriten, Verbindungsdaten und Logs.")
}

' Externes System
System_Ext(mail, "E-Mail-System", "SMTP-Server", 
  "Versendet Benachrichtigungen und Passwort-Resets.")

' Beziehungen
Rel(person, web, "nutzt die Weboberfläche", "HTTPS")
Rel(web, api, "ruft REST-Endpunkte auf", "HTTPS / JSON")
Rel(api, db, "liest und schreibt Daten", "JDBC / SQL")
Rel(api, mail, "sendet E-Mails über", "SMTP")

' Rückflüsse
Rel_D(api, web, "liefert Suchergebnisse, Bestätigungen und Fehlerhinweise")

' Ausgabe: Containerdiagramm
SHOW_LEGEND()
@enduml
