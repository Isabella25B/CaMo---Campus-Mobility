# Datenbank-Konfiguration
# HINWEIS: Dies ist nur eine Vorlage! Die echte .env Datei wird NICHT ins Git committed.
# Kopiere diese Datei zu .env und fülle mit deinen Werten aus.

DB_NAME=database_name
DB_USER=user_name
DB_PASS=user_password
DB_HOST=host_ip
DB_PORT=port
